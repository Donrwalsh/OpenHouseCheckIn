# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'OpenHouseCheckIn' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for OpenHouseCheckIn
	source 'https://github.com/CocoaPods/Specs.git'
	platform :ios, '9.0'
	use_frameworks!

	pod 'Eureka'

  target 'OpenHouseCheckInTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'OpenHouseCheckInUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
